# Introduction
Now that we have a running backend service that interacts with our database, we want to add a frontend to allow users to interact with the application. This time we will explore the new "Plan" mode to create a clear and actionable plan before starting the implementation.

# Task
- Before you start implementing the frontend, take you 3 minutes and watch this [short video](https://www.youtube.com/watch?v=xR3fuKtWGN0) which explains the Plan mode in GitHub Copilot very well.
- Now switch to the Plan mode in Visual Studio Code and ask GitHub Copilot to create a plan for adding a frontend to our application. In the prompt, stay short to allow to get to know the capabilities of the planning agent. The prompt could look like this: "Create a plan to add a frontend to the existing application in the `frontend` folder that fullfills the requirements given in the Readme.md file." Let 
- Make sure that the plan follows the Api first approach and uses the openapi specification that we created in a previous task to generate the http client for the frontend.

